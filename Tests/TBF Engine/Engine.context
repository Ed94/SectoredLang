context : "Engine";
LTP     : Interpreter ;
layers  : Explicit ;

dependencies :
PAL : bp, 
;

alias :
OS      : PAL.OSAL;
Console : PAL.Console;
;

units : 
	Cycler     : bp, ;
	Execution  : bp, spec;			
	Engine     ;
	Input      : bp, spec;
	Memory     : bp, spec;
	Renderer   : bp, spec;
	State      : bp, spec;
	State.Intro: bp, spec;
	Timing     : bp, spec;
;

entrypoint : Entrypoint;