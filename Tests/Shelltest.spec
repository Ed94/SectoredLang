// Shell Script

// Shell script is jit compiled live and will get information per statement the user completes.
// All statements end wit ; so it will continue to gather lines of information until the token is reached.
// A statement will be processed and be processed immediately. If its a definition instead of a command, 
// it will be stored as live scratch library of definitions.
// The user will be able to view this scratch library's definition tree and decide if it should be broken up into
// units of library packages or as a package itself with a name specified.
